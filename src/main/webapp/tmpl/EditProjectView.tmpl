<script id="tmpl-EditProjectView" type="text/html">
	<div class="editProjectView">
	</div>
	<div class="modal modalContent">
		<div class="modal-header">
    		<button type="button" class="close">&times;</button>
    		<h3>Edit Project for {{project.name}}</h3>
  		</div>
  		<div class="form-inline" class="modal-footer" data-project-id="{{project.id}}">
			<input type="text" name="ticketName" value="{{project.name}}" class="ticketTextFiled" placeholder="Enter the Ticket Name"/>
			<button class="btn btn-primary updateProjectBtn" type="button">Update</button>
		</div>
	</div>
	
</script>